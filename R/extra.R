  # Requires DataTables version >= 1.10
  #renderDT(datatable(tab, list(searching = FALSE, paging = FALSE, info = FALSE,
  #                   ordering = FALSE), "compact", rownames = FALSE,
  #                   escape = FALSE, selection = "none"))
