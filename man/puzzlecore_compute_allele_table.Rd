% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/puzzlecore_process.R
\name{puzzlecore_compute_allele_table}
\alias{puzzlecore_compute_allele_table}
\title{Compute Allele Table for Pedigree
This function computes the allele count table for a given pedigree and inheritance model.}
\usage{
puzzlecore_compute_allele_table(pedigree, inher)
}
\arguments{
\item{pedigree}{A list of pedigree entries, each containing sample_id, kinship, status, sex, and code.}

\item{inher}{The inheritance model as a string.}
}
\value{
A named list where each name is a sample_id and the value is the corresponding allele count.
}
\description{
Compute Allele Table for Pedigree
This function computes the allele count table for a given pedigree and inheritance model.
}
\examples{
pedigree <- list(
  list(sample_id = "S1", kinship = "proband", status = "affected", sex = "male", code = 1),
  list(sample_id = "S2", kinship = "father", status = "unaffected", sex = "male", code = 2),
  list(sample_id = "S3", kinship = "mother", status = "unaffected", sex = "female", code = 3)
)
allele_table <- puzzlecore_compute_allele_table(pedigree, "Homozygous Recessive")
print(allele_table)
}
