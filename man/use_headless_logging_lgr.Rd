% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logging_session.R
\name{use_headless_logging_lgr}
\alias{use_headless_logging_lgr}
\title{Use minimal headless logging (lgr only; no token/ip/path/user fields)}
\usage{
use_headless_logging_lgr(layout = "\%l [\%t] \%m", threshold = NULL)
}
\arguments{
\item{layout}{Format string for console (default: \code{\%l [\%t] \%m}).
Tokens: \code{\%l} = level, \code{\%t} = timestamp, \code{\%m} = message.}

\item{threshold}{Optional logging threshold override (e.g., "info", "debug").
NULL leaves existing threshold unchanged.}
}
\value{
TRUE (invisibly)
}
\description{
Call once in a non-Shiny/headless context (e.g. inside run_puzzle_pipeline)
to suppress per-session fields and simplify console output.
}
\details{
Effects:
\itemize{
\item Disables token/ip/path/user via set_log_privacy(FALSE, ...)
\item Ensures the 'shinyapp' logger has a console appender with a minimal layout.
\item Does NOT alter file appenders (session JSONL logs keep full structured fields
unless you also call set_log_privacy(FALSE, ...) before starting session loggers).
}

If you want JSON files ALSO to omit those fields, call use_headless_logging_lgr()
BEFORE start_session_logger().
}
